name: Workbench Functional Smoke Temp Token

permissions:
  contents: read

on:
  push:
    branches:
      - evaos/beta-rc-20260612
    paths:
      - .github/workflows/workbench-functional-smoke-temp-token.yml

concurrency:
  group: workbench-functional-smoke-temp-token-${{ github.ref_name }}
  cancel-in-progress: true

env:
  BUN_INSTALL_REGISTRY: https://registry.npmjs.org/
  CI: true
  CSC_IDENTITY_AUTO_DISCOVERY: false
  EVAOS_BETA_PUBLIC_RELEASE: 'false'
  EVAOS_BETA_REQUIRE_SIGNING: 'false'
  EVAOS_FINALIZE_MAC_DMG: 'false'
  EVAOS_DMG_CODESIGN: 'false'
  ELECTRON_BUILDER_COMPRESSION_LEVEL: '1'
  NODE_OPTIONS: '--max-old-space-size=8192'
  WORKBENCH_SMOKE_BRIDGE_REF: d58e543084ae4d103a4a60f2858b5fdb18a209f8

jobs:
  macos-arm64-app:
    name: macOS arm64 unpacked app
    runs-on: macos-14
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
          persist-credentials: false

      - id: commit
        shell: bash
        run: |
          set -euo pipefail
          echo "short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/cache@v4
        with:
          path: |
            ${{ runner.temp }}/.cache/electron
            ${{ runner.temp }}/.cache/electron-builder
            ~/.cache/electron
            ~/.cache/electron-builder
          key: workbench-functional-smoke-electron-arm64-${{ hashFiles('package.json', 'bun.lock') }}
          restore-keys: workbench-functional-smoke-electron-arm64-

      - uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: bun install --frozen-lockfile

      - run: bun run postinstall || true
      - run: bunx electron-builder install-app-deps
        env:
          npm_config_runtime: electron
          npm_config_disturl: https://electronjs.org/headers
          ELECTRON_CACHE: ${{ runner.temp }}/.cache/electron
          ELECTRON_BUILDER_CACHE: ${{ runner.temp }}/.cache/electron-builder

      - name: evaOS guardrail audit
        shell: bash
        run: |
          set -euo pipefail
          node scripts/evaosBetaReleaseGate.js audit-config
          node scripts/evaosUpstreamGuardrailAudit.js

      - name: Build unsigned unpacked app only
        shell: bash
        env:
          EVAOS_DESKTOP_BRIDGE_DISABLE_DEFAULT_CANDIDATES: '1'
          EVAOS_DESKTOP_BRIDGE_SOURCE_REF: ${{ env.WORKBENCH_SMOKE_BRIDGE_REF }}
          EVAOS_DESKTOP_BRIDGE_SOURCE_TOKEN: ${{ secrets.EVAOS_DESKTOP_BRIDGE_SOURCE_TOKEN || secrets.GH_TOKEN || secrets.GITHUB_TOKEN || github.token }}
        run: |
          set -euo pipefail
          echo "NODE_OPTIONS=${NODE_OPTIONS:-}"
          node scripts/build-with-builder.js arm64 --mac dir --arm64

      - id: verify
        name: Verify staging app shape
        shell: bash
        run: |
          set -euo pipefail
          APP_PATH="$(find out -type d -name '*.app' -print -quit)"
          test -n "$APP_PATH"
          BRIDGE="$APP_PATH/Contents/Resources/Bridge/evaos-desktop-bridge"
          MANIFEST="$APP_PATH/Contents/Resources/Bridge/manifest.json"
          test -x "$BRIDGE"
          test -f "$MANIFEST"
          test -d "$APP_PATH/Contents/Resources/bundled-aioncore"
          test -d "$APP_PATH/Contents/Resources/hub"
          BUNDLE_ID="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$APP_PATH/Contents/Info.plist")"
          VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$APP_PATH/Contents/Info.plist")"
          BRIDGE_VERSION="$(node -e "const fs=require('fs'); const m=JSON.parse(fs.readFileSync(process.argv[1],'utf8')); console.log(m.sourceCommit || m.commit || m.version || 'unknown')" "$MANIFEST")"
          test "$BRIDGE_VERSION" = "$WORKBENCH_SMOKE_BRIDGE_REF"
          echo "app_path=$APP_PATH" >> "$GITHUB_OUTPUT"
          echo "bundle_id=$BUNDLE_ID" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "bridge_version=$BRIDGE_VERSION" >> "$GITHUB_OUTPUT"
          {
            echo "## Workbench Functional Smoke Temp Token"
            echo "- SHA: \`${{ steps.commit.outputs.sha }}\`"
            echo "- Version: \`$VERSION\`"
            echo "- Bundle ID: \`$BUNDLE_ID\`"
            echo "- Bridge manifest: \`$BRIDGE_VERSION\`"
            echo "- Unsigned/unnotarized staging app only."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Archive staging app
        shell: bash
        env:
          APP_PATH: ${{ steps.verify.outputs.app_path }}
          SHORT: ${{ steps.commit.outputs.short }}
        run: |
          set -euo pipefail
          ARTIFACT="workbench-functional-smoke-$SHORT-mac-arm64.zip"
          ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" "$ARTIFACT"
          shasum -a 256 "$ARTIFACT" | tee "$ARTIFACT.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: workbench-functional-smoke-macos-arm64-${{ steps.commit.outputs.short }}
          path: |
            workbench-functional-smoke-${{ steps.commit.outputs.short }}-mac-arm64.zip
            workbench-functional-smoke-${{ steps.commit.outputs.short }}-mac-arm64.zip.sha256
          retention-days: 7
